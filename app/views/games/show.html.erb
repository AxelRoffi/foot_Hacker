<!-- <body>
      <div class="card">
          <div class="photo"></div>
          <div class="banner"></div>
          <ul>
              <li><b></b></li>
              <li></li>
            </ul>
            <button class="contact" id="main-button">click to get in touch</button>
          <form class="email-form">
                <input id="name" type="text" placeholder="name">
                <input id="email" type="text" placeholder="email">
                <textarea id="comment" type="text" placeholder="comment"></textarea>
                <button class="contact">send</button>
          </form>
          </div>
    </body>
 -->

<ul>
<% @local_team_players.each do |lplayer| %>
     <li> <%= lplayer.player.last_name%> </li>
 <% end %>
</ul>


 <% @visitor_team_players.each do |vplayer| %>
       <%= vplayer.player.last_name %>
  <% end %>

